1) npm init -y --> automatically give details
2) import and require are same
3) "type": "module" --> by writing this in package.json we use 'import'
4) npm install --save-dev nodemon --> we use only for our work. And it will not work for other when we push on server
5)  


LINKS
1) http://localhost:5000/
2) http://localhost:5000/users